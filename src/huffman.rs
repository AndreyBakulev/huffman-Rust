pub struct Huffman{

}
impl Huffman{
    pub fn new(){

    }
    pub fn encode(string: String){
        /*
        1. get amt of each char
        then make a btree based off char combinations
        plot 0's and 1's
        make dictionary based off binary val + char
        read the string again and translate it to the binary string
        finish :D
        */
    }
}